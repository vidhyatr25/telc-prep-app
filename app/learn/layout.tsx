export const metadata = {
  title: "DeutschLearn Courses",
  description: "German A1 and A2 TELC preparation courses in DeutschLearn.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
