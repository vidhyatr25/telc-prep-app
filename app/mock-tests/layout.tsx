export const metadata = {
  title: "DeutschLearn Mock Tests",
  description: "TELC-style German mock tests in DeutschLearn.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
